diff --git a/content/common/features.cc b/content/common/features.cc
index 7e7515685e7e7adde2525982ae94335181928ed4..53b56a435de5be77b7161d9625adce86fc09d84b 100644
--- a/content/common/features.cc
+++ b/content/common/features.cc
@@ -583,8 +583,9 @@ BASE_FEATURE(kPriorityOverridePendingViews, base::FEATURE_ENABLED_BY_DEFAULT);
 
 // Enables exposure of the core milestone 1 (M1) APIs in the renderer without an
 // origin trial token: Attribution Reporting, FLEDGE, Topics.
+// kPrivacySandboxAdsAPIsM1Override feature state is enforced via plaster rewrite.
 BASE_FEATURE(kPrivacySandboxAdsAPIsM1Override,
-             base::FEATURE_ENABLED_BY_DEFAULT);
+             base::FEATURE_DISABLED_BY_DEFAULT);
 
 #if BUILDFLAG(IS_ANDROID)
 // When disabled("legacy behavior") it resets ongoing gestures when window loses
