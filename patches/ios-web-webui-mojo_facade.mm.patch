diff --git a/ios/web/webui/mojo_facade.mm b/ios/web/webui/mojo_facade.mm
index 4e7ecb965cb4066179d044d78307b2216a6b9e7b..81f555987c724938efae0bfe11403d13dce7dedb 100644
--- a/ios/web/webui/mojo_facade.mm
+++ b/ios/web/webui/mojo_facade.mm
@@ -169,6 +169,8 @@ void MojoFacade::HandleMojoMessage(
     int message_id,
     const base::DictValue* message,
     base::OnceCallback<void(int, std::string)> completion) {
+  base::AutoReset<std::string> frame_scope(&current_frame_id_,
+                                           GetFrameIdForMessage(message));
   DCHECK_CURRENTLY_ON(WebThread::UI);
 
   const std::string* name = message->FindString("name");
@@ -231,6 +233,7 @@ void MojoFacade::HandleMojoMessage(
 }
 
 bool MojoFacade::HandleMojoBindInterface(const base::DictValue& args) {
+  if (!IsBindInterfaceAllowedForFrame(args)) return false;
   const std::string* interface_name = args.FindString("interfaceName");
   std::optional<int> pipe_id = FindIntOrDoubleAsInt(args, "requestHandle");
 
@@ -360,7 +363,9 @@ void MojoFacade::OnWatcherCallback(int callback_id,
                                    int watch_id,
                                    int pipe_id,
                                    MojoResult result) {
-  WebFrame* main_frame = GetMainWebFrame();
+  base::AutoReset<std::string> frame_scope(&current_frame_id_,
+                                           GetFrameIdForWatch(watch_id));
+  WebFrame* main_frame = GetFrameForWatch(watch_id);
   if (!main_frame) {
     return;
   }
@@ -395,6 +400,7 @@ void MojoFacade::OnWatcherCallback(int callback_id,
 }
 
 base::Value MojoFacade::HandleMojoHandleWatch(const base::DictValue& args) {
+  base::Value result = [&]() -> base::Value {
   std::optional<int> pipe_id = FindIntOrDoubleAsInt(args, "handle");
   CHECK(pipe_id.has_value());
   std::optional<int> signals = FindIntOrDoubleAsInt(args, "signals");
@@ -416,9 +422,19 @@ base::Value MojoFacade::HandleMojoHandleWatch(const base::DictValue& args) {
   watcher->ArmOrNotify();
   watchers_.insert(std::make_pair(watch_id, std::move(watcher)));
   return base::Value(watch_id);
+  }();
+  if (const std::string* frame_id = args.FindString("frameId")) {
+    if (std::optional<int> watch_id = result.GetIfInt()) {
+      watch_id_to_frame_id_[*watch_id] = *frame_id;
+    }
+  }
+  return result;
 }
 
 void MojoFacade::HandleMojoWatcherCancel(const base::DictValue& args) {
+  if (std::optional<int> watch_id = FindIntOrDoubleAsInt(args, "watchId")) {
+    watch_id_to_frame_id_.erase(*watch_id);
+  }
   std::optional<int> watch_id = FindIntOrDoubleAsInt(args, "watchId");
   CHECK(watch_id.has_value());
   watchers_.erase(*watch_id);
@@ -427,39 +443,48 @@ void MojoFacade::HandleMojoWatcherCancel(const base::DictValue& args) {
 int MojoFacade::AllocatePipeId(mojo::ScopedMessagePipeHandle pipe,
                                std::optional<int> custom_id) {
   int pipe_id = custom_id.value_or(next_pipe_id_--);
-  pipes_[pipe_id] = std::move(pipe);
+  StorePipeForCurrentFrame(pipe_id, std::move(pipe));
   return pipe_id;
 }
 
 mojo::MessagePipeHandle MojoFacade::GetPipeFromId(int id) {
-  auto it = pipes_.find(id);
-  if (it == pipes_.end()) {
+  auto it = CurrentFramePipes().find(id);
+  if (it == CurrentFramePipes().end()) {
     return {};
   }
   return it->second.get();
 }
 
 mojo::ScopedMessagePipeHandle MojoFacade::TakePipeFromId(int id) {
-  auto it = pipes_.find(id);
-  if (it == pipes_.end()) {
+  auto it = CurrentFramePipes().find(id);
+  if (it == CurrentFramePipes().end()) {
     return {};
   }
 
   mojo::ScopedMessagePipeHandle pipe = std::move(it->second);
-  pipes_.erase(it);
+  CurrentFramePipes().erase(it);
   return pipe;
 }
 
 void MojoFacade::WebFrameBecameAvailable(WebFramesManager* web_frames_manager,
                                          WebFrame* web_frame) {
+  [&]() -> void {
   DCHECK_CURRENTLY_ON(WebThread::UI);
   if (web_frame && web_frame->IsMainFrame()) {
     main_frame_ = web_frame->AsWeakPtr();
   }
+  }();
+  if (web_frame && !web_frame->IsMainFrame() &&
+      !web_state_->IsLoading() &&
+      web_state_->GetInterfaceBinderForMainFrame()
+          ->HasRegisteredInterfaces()) {
+    AwaitNextMessageForFrame(web_frame->GetFrameId());
+  }
 }
 
 void MojoFacade::WebFrameBecameUnavailable(WebFramesManager* web_frames_manager,
                                            const std::string& frame_id) {
+  [&]() -> void {
   DCHECK_CURRENTLY_ON(WebThread::UI);
   if (main_frame_ && main_frame_->GetFrameId() == frame_id) {
     main_frame_.reset();
@@ -468,10 +493,16 @@ void MojoFacade::WebFrameBecameUnavailable(WebFramesManager* web_frames_manager,
     is_awaiting_message_ = false;
     weak_ptr_factory_.InvalidateWeakPtrs();
   }
+  }();
+  extra_polling_frame_ids_.erase(frame_id);
+  EraseWatchersForFrame(frame_id);
+  pipes_.erase(frame_id);
 }
 
 void MojoFacade::PageLoaded(WebState* web_state,
                             PageLoadCompletionStatus load_completion_status) {
+  extra_polling_frame_ids_.clear();
+  watch_id_to_frame_id_.clear();
   DCHECK_CURRENTLY_ON(WebThread::UI);
   DCHECK_EQ(web_state_, web_state);
   if (load_completion_status == PageLoadCompletionStatus::SUCCESS &&
@@ -487,6 +518,8 @@ void MojoFacade::PageLoaded(WebState* web_state,
 }
 
 void MojoFacade::WebStateDestroyed(WebState* web_state) {
+  extra_polling_frame_ids_.clear();
+  watch_id_to_frame_id_.clear();
   DCHECK_CURRENTLY_ON(WebThread::UI);
   DCHECK_EQ(web_state_, web_state);
 
